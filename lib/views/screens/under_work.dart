import 'package:flutter/material.dart';
import 'package:letterboxd_porto_3/helpers/style.dart';

class UnderWorkScreen extends StatelessWidget {
  const UnderWorkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Under Development", style: boldText,),
    );
  }
}